# Week 5 — 도입 전/후 비교 리포트

실험 대상: `agentic-lab` 저장소("오늘의 J-POP 한 곡 뽑기" 미니 프로젝트).
같은 유형의 질문("새 곡 카테고리를 추가하려면 어떻게 해야 해?")을, CLAUDE.md/GOLDEN_RULES.md가 없을 때(STEP 2)와 심은 뒤 완전히 새로운 세션에서(STEP 7) 각각 물어 답변을 비교했다. (STEP 7에서는 STEP 6에서 이미 실제로 추가해버린 "발라드" 대신, 같은 유형의 새 카테고리 "시부야케이"로 질문했다.)

## 도입 전 (규칙 파일 없음) — "발라드 카테고리를 추가하고 싶어"

> "발라드" 카테고리를 추가하려면 총 2개 파일만 수정하면 됩니다. `app.js`와 `style.css`는 카테고리 이름에 하드코딩된 부분이 없어서 그대로 두어도 동작합니다.
>
> **1. `songs.js` — 데이터 추가**
> `SONGS_BY_CATEGORY` 객체에 `ballad` 키를 새로 추가하고, 곡 목록을 배열로 넣습니다. 파일 맨 위 주석에 "곡 데이터는 반드시 이 객체에만 추가"라고 명시되어 있으므로, 곡 추가는 여기서만 하면 됩니다.
>
> **2. `index.html` — 탭 버튼 추가**
> `.category-tabs` 안에 다른 버튼들과 동일한 패턴으로 버튼을 하나 추가합니다. `data-category` 값이 `songs.js`의 키(`ballad`)와 정확히 일치해야 합니다.
>
> **3. `app.js` — 수정 불필요**
> 카테고리 이름을 하드코딩하지 않고 `btn.dataset.category` 값을 그대로 조회하는 구조라 자동으로 동작합니다.
>
> **4. `style.css` — 수정 불필요 (선택 사항)**
> 클래스 기반 스타일이라 자동 적용됩니다. 버튼이 늘어나면 `flex-wrap: wrap;` 정도만 고려하면 됩니다.

## 도입 후 (CLAUDE.md + GOLDEN_RULES.md 심은 뒤, 새 세션) — "시부야케이 카테고리를 추가하고 싶어"

> "시부야케이" 카테고리를 추가하려면 GOLDEN_RULES.md의 규칙에 따라 아래 두 파일만 수정하면 됩니다. `app.js`와 `style.css`는 건드릴 필요가 없습니다.
>
> **1. `songs.js` 수정 (규칙 1, 2)**
> 곡 데이터는 반드시 `songs.js`의 `SONGS_BY_CATEGORY` 객체에만 추가합니다. `app.js`에는 곡 데이터를 직접 적지 않습니다.
>
> **2. `index.html` 수정 (규칙 3, 4)**
> `data-category` 값(`shibuyakei`)은 `songs.js`에 추가한 키 이름과 정확히 일치해야 합니다 (규칙 3). 버튼 라벨은 "시부야케이"처럼 한글 단독 표기를 쓰지 않고, J-POP/J-ROCK처럼 영문 약칭(예: `SHIBUYA-KEI`)으로 통일합니다 (규칙 4).
>
> **3. `app.js`는 수정 불필요**
> `SONGS_BY_CATEGORY`와 `data-category` 값을 동적으로 참조하는 구조라, 새 카테고리를 키와 버튼으로만 추가하면 로직 변경 없이 자동으로 동작합니다.
>
> **4. `style.css`는 선택 사항**
> 이미 공통 스타일이 적용되므로 그대로 두면 됩니다.
>
> **5. 마지막 확인 (규칙 5)**
> 수정 후 `index.html`을 직접 열어 SHIBUYA-KEI 버튼을 클릭하고 "한 곡 뽑기"를 눌러 눈으로 확인합니다.

## 관찰

1. 도입 전 답변은 코드에 남아있던 주석 하나("곡 데이터는 이 객체에만 추가")에 의지해 근거를 댔지만, 도입 후 답변은 GOLDEN_RULES.md의 규칙 번호(1~5)를 하나하나 짚어가며 "왜 이렇게 해야 하는지"를 명시적으로 설명했다.
2. 도입 전에는 카테고리 버튼 라벨을 어떻게 지어야 하는지 아무 언급이 없었다 — 실제로 STEP 1(규칙 없는 상태)에서 "시티팝"이라는 한글 라벨이 섞여 들어가는 일관성 문제가 그대로 발생했다. 도입 후에는 "영문 약칭으로 통일, 한글 단독 표기 금지"라는 규칙을 먼저 확인하고 답변에 반영했다.
3. 도입 전 답변엔 "수정 후 확인" 절차가 아예 없었지만, 도입 후 답변에는 "index.html을 직접 열어 눈으로 확인"하라는 마지막 단계가 규칙 5번으로 빠짐없이 포함됐다.
4. 두 답변 모두 고쳐야 할 파일(`songs.js`, `index.html`)은 똑같이 짚어냈다 — 즉 규칙 파일이 없어도 코드만 보고 기본 구조는 파악할 수 있었지만, 라벨 표기·확인 절차 같은 "말로 정해두지 않으면 매번 달라질 수 있는" 세부 규칙은 문서화해야만 답변에서도, 실제 코드에서도 안정적으로 지켜졌다.
